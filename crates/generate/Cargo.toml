[package]
name = "agent-loadgen-generate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic coding-agent scenario generation for agent-loadgen"
repository.workspace = true
publish = false

[dependencies]
agent-loadgen-core = { version = "0.1.0", path = "../core" }
anyhow = "1"
hex = "0.4"
rand = "0.9"
rand_distr = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
toml = "0.9"
